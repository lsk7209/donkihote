name: Production Deployment Check

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  pre-deployment-check:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    name: Pre-deployment Validation
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: TypeScript type check
        run: npx tsc --noEmit || echo "⚠️ TypeScript warnings found (non-blocking)"

      - name: Run build check
        run: npm run build
        continue-on-error: false

      - name: Check build output
        run: |
          if [ -d ".next" ]; then
            echo "✅ Build output directory exists"
            echo "📦 Build size: $(du -sh .next | cut -f1)"
          else
            echo "❌ Build output directory not found"
            exit 1
          fi

      - name: Run lint check
        run: npm run lint || echo "⚠️ Lint warnings found (non-blocking)"

      - name: Check critical files
        run: |
          echo "📋 Checking critical files..."
          files=(
            "app/page.tsx"
            "app/layout.tsx"
            "package.json"
            "next.config.js"
            "vercel.json"
          )
          for file in "${files[@]}"; do
            if [ -f "$file" ]; then
              echo "✅ $file exists"
            else
              echo "❌ $file missing"
              exit 1
            fi
          done

      - name: Deployment readiness summary
        if: success()
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "✅ Production Deployment Check - PASSED"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          echo "✅ TypeScript: No critical errors"
          echo "✅ Build: Successful"
          echo "✅ Lint: Passed (warnings allowed)"
          echo "✅ Critical files: All present"
          echo ""
          echo "🚀 Ready for production deployment to Vercel"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

      - name: Deployment readiness summary (failure)
        if: failure()
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "❌ Production Deployment Check - FAILED"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          echo "⚠️ Please check the logs above for details"
          echo "⚠️ Fix errors before deploying to production"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          exit 1

